#!/bin/sh
# Copy to env.sh (gitignored) and fill in. Then `source env.sh` before running apply.
export GRAYLOG_URL="https://graylog.example.com/api"
export GRAYLOG_TOKEN="paste-token-here"
# If using a self-signed cert, the apply scripts pass -k unconditionally.
